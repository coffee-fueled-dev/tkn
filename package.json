{
  "name": "@tkn/monorepo",
  "version": "0.1.0-beta.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "tools/*"
  ],
  "scripts": {
    "context": "bun run --filter @tkn/scripts context",
    "corpus:tinystories": "bun run --filter @tkn/corpora corpus:tinystories",
    "corpus:brown": "bun run --filter @tkn/corpora corpus:brown"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {},
  "trustedDependencies": [
    "kuzu"
  ]
}
